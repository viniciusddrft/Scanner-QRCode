import 'package:flutter/material.dart';

class SideBarQrcodeWidget extends StatelessWidget {
  const SideBarQrcodeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black.withAlpha(192),
    );
  }
}
