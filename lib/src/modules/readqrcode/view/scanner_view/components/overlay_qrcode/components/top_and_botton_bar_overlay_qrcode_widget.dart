import 'package:flutter/material.dart';

class TopAndBottonBarQrcodeWidget extends StatelessWidget {
  const TopAndBottonBarQrcodeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black.withAlpha(192),
    );
  }
}
