import 'package:flutter/material.dart';

class SideBarBarcodeWidget extends StatelessWidget {
  const SideBarBarcodeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black.withAlpha(192),
    );
  }
}
